{
    "platform" : "${out value=specification.platform /}",
    "sub_platforms" : ${out value=specification.sub_platforms /},
    "id" : "${out value=specification.id /}",
    "name" : "${out value=specification.name /}",
    "short_name" : "${out value=specification.short_name /}",
    "description" : "${out value=specification.description /}",
    "version" : "${out value=specification.version /}",
    "author" : "${out value=specification.author /}",
    "capabilities" : ${out value=specification.capabilities /},
    "capabilities_allowed" : ${out value=specification.capabilities_allowed /},
    "dependencies" : ${out value=specification.dependencies /},
    "main_file" : "${out value=specification.main_file /}",
    "resources" : ${out value=specification.resources /}
}
